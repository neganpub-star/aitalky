# 平台后管(admin)功能清单

> 平台运营方后台(apps/admin,launcher admin 8090),非租户(项目)后台。
> 此处汇总「将来要放到后管编辑/维护」的功能,开发平台后管时按此落地。
> 关联:`开发进度清单.md` 的「平台后管(admin)」条目;参考截图 `doc/bytrack/images/live-现网截图/06-平台后管/`。

> 进度(后端 API 全部跑通并本地验证;前端 apps/admin 尚未开发):
> - 登录/用户/项目/套餐/加量包/协议/语种字典 的后端接口均已落地(端口 8090,/api/admin/**)。
> - 默认平台管理员:`admin` / `admin123456`(V6 迁移,上线必改)。

## 一、平台基础(进度清单已列)
- [x] 平台管理员登录(pf_admin)—— 用户名+RSA密码+图形验证码,scope=platform JWT
- [x] 用户管理 / 项目管理 —— 跨租户分页/搜索/详情/启停(AdminViewService)
- [x] 套餐 / 加量包管理 —— CRUD + 上下架(含套餐配额一体维护)
- [x] 协议管理 —— 三件套×多语言 CRUD
- [ ] 订阅 / 订单查看 —— 待 billing 模块落地(pay_ 表当前无数据)
- [ ] **前端 apps/admin**(登录 + 各管理页,含多语言 + 主题切换)—— 未开发

## 二、字典 / 基础数据维护
- [x] **语种字典维护(新增,已落地)** —— 平台维护「系统支持的语言」全集(语言编码 + 中英文名 + 排序 + 启停)。
  - 已建表 `pf_language`(V7,18 语种种子);后管 CRUD `/api/admin/languages`(权限 dict);console 启动读 `/api/languages` 覆盖本地种子(`constants/languages.ts` 改为 API 驱动+兜底)。
  - 背景:参考系统 ByteTrack 的语种是读后端接口(`getAppLanguage` 返回 `{id, appId, language, type}`,type=1 默认/0 其他),按项目存表。
  - 现状(本期落地):项目级启用语种已建表 `mse_messenger_language`(project_id, language, type),租户在「常规设置 → 选择支持的语言」里增删/设默认;**可选语种全集(新增语言的候选项)暂用前端常量** `apps/console/src/constants/languages.ts`。
  - 后管要做:把"可选语种全集"从前端常量迁到**平台级语种字典表**(如 `pf_language`),后管可新增/编辑/停用语种;console 的语种候选从后端接口读取,实现"加新语种无需改代码"。
  - 迁移点:`constants/languages.ts` → 后端字典接口(单一来源,前端已集中,替换即可)。

## 三、其他(后续补充)
- [ ] (按需补充)
