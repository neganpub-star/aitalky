# aitalky 开发进度清单

> 打勾=已完成并验证。新会话从"进行中/待办"接着干。规则见根目录 `CLAUDE.md`。
> 更新于 2026-06-22(订阅计费扩展包全做 + 统一配额校验 + 后管订阅管理 完成;新增 `aitalky-parent/sql/` 库快照+清库脚本)。

## 一、地基(全部完成)
- [x] 工程骨架:Maven 模块化单体(base/modules/launcher)+ 多入口(app/admin/ws)
- [x] 统一响应 R / 错误码 / 全局异常 / i18n(中英)
- [x] 多租户隔离(project_id 自动注入)+ RBAC 功能权限 `@RequiresFunction`
- [x] JWT 无状态鉴权 + TenantContext;雪花ID;审计填充;逻辑删除
- [x] 分布式锁 / 分布式定时;Long→String 序列化;统一时间格式
- [x] Flyway 迁移(V1建表26/V2种子/V3操作日志)
- [x] 可观测性:traceId + logback 滚动 + 操作日志 `@Log`→sys_oper_log
- [x] 限流 `@RateLimit`;Knife4j 文档(/doc.html);MinIO 文件上传
- [x] 密码 RSA 加密传输;邮箱验证码 2FA(万能码 888888)
- [x] 前端地基:i18n(react-i18next)+ Zustand + ESLint/Prettier + 暗色主题

## 二、后端业务
- [x] identity:注册/登录(2FA)/创建项目(自动建3系统角色+owner)/进入项目/成员管理(列表·调整角色·改昵称头像·启用禁用·删除,负责人保护)/角色列表
- [x] customer:业务UID/游客聚合,随机名头像
- [x] 会话(P1):建会话/未分配池/收件箱视图/认领/结束/重开/未读
- [x] 消息(P1):Mongo + Redis seq + 发送者快照 + 内部消息过滤;坐席/信使 REST
- [x] WS 实时(P2):app→RocketMQ→ws 推送坐席+客户(代看/代发由订阅覆盖)
- [x] 信使配置(mse_messenger):品牌/问候语/多语言内容/紧急通知 后管编辑 + 信使端读取
  - 行为开关已下发并生效:系统消息显示(未读分割线/typing/成员撤回)、偏好(失焦弹窗+提示音)、客户撤回权限。
  - **消息撤回**(坐席+客户,isVisible=false,2分钟时限)、**typing 正在输入中**(双向 WS 瞬时,不落库) 已随此模块一并落地。
  - 待用户验收:信使设置主页/常规设置 1:1 像素核对;启动器样式卡片(待截 07-启动器样式);popupAllowClose 待启动器悬浮气泡再接。
- [x] 客服组/分配引擎(asn_):普通/专属组 + 手动/轮流/负载 + 承载量 + group_key URL 接入
  - **P1 普通分配**(commit 50ff6ac)+ **P2 专属分配**(commit cdc3714)均已落地;详见 `doc/design/会话分配规则设计.md` §8/§9。
  - 接入 URL 格式:`https://msg.<域名>/?groupId=<专属分配标识>&appId=<项目appId>`(&userId 可选)。
  - **groupId = 每条「专属分配策略」生成的接入标识**(会话设置→专属分配模式→查看代码/URL 列复制);客户从哪条 URL 进 → 分到该策略的队友。appId 项目级唯一。
  - 信使端 init 已按 groupId 反解专属策略并在该策略队友内分配;非法 groupId 降级普通分配。
  - 待补:信使端「专属名」头部(缺参考截图)、详情面板「来源渠道名称」、删策略时进行中会话校验(见 §9 未做项)。
- [x] 会话超时结束系统消息:保持期 job(`CloseIdleConversationJob`)关会话时发「会话超时结束」系统消息(`AssignNotifier.notifyTimeoutClosed`,internal=true 仅坐席可见,信使端 sync 过滤),进 Mongo+WS 仅推坐席+推进 lastSeq/列表预览;坐席端 system 居中渲染。✅已完成
- [ ] 翻译(AI翻译):客户消息实时翻译 + 译文缓存 + 计费扣字符
- [x] 已读回执(客户 `customer_read_seq` 上报→坐席消息显示"已读")/ 撤回(见信使配置)/ @提及 mention(`Message.mentions[]` + view=mention 视图/计数 + 发送 @ + pushMention 推送)✅已完成
- [x] **黑名单(sup_blacklist)**:详情面板拉黑(用户=全设备/游客=该设备)+ 信使 init 拦截 + 信使设置黑名单页(移出)
- [x] **快捷回复(sup_quick_reply)**:聊天区工具栏 Popover 插入 + 信使设置管理页(CRUD);分类后端支持,管理页 MVP 未做分类 UI
- [x] **个人中心(account)**:基本资料(改昵称/头像-MinIO上传/退出项目)+ 偏好设置(语言)+ 系统推送(声音/桌面);图标栏/头像菜单显示成员头像昵称
- [x] **MinIO 桶匿名只读**:修复头像/图片 <img> 403
- [x] **订阅计费(billing,bil_)**:产品流程对齐 ByteTrack,支付用 Coinly 钱包(自研对接,不引jar)。分5期 + 加购:
  - **扩展包全做 + 统一配额校验 ✅完成(2026-06-18,commit 33ba9c8…348372b)**:对齐参考把三个扩展包(翻译/AI Tokens/客户配额)做全 + 建统一资源校验体系。详细决策见记忆 `billing-extension-packs-todo`。
    - **迁移**:V30(套餐基础席位统一→1,现有订阅旧基础席位转加购保有效席位不变;翻译包改价$69;新增 AI Tokens 加量包 100万=$20;**新建 `bil_project_resource` 项目级永久配额表**;迁移旧 `bil_subscription.extra_customers`→新表)、V31(bil_order 加 `addon_packs` 列,订阅单搭售包)、V32(席位包价$30→$20)。
    - **公共 `QuotaService`**(billing):`limit/hasRemaining/ensure/grantPack/setPack`。口径=套餐型(seat/article/site)走套餐配额+席位加购;永久包(customer/translate_char/ai_tokens)=免费默认(default_*)+已购包(bil_project_resource)。limit 只依赖 billing+platform,used 由调用方传入(避免与 identity/customer 循环依赖)。新增 ResultCode `RESOURCE_QUOTA_EXCEEDED`。
    - **统一拦截**:identity 依赖 billing,`InviteService.createMember` 加成员前 `ensure(seat)` 超额拦截(未订阅=1免费席位)。客户入站自动建**未硬拦**(避免打断消息),待定。
    - **扩展包免订阅购买**:`createAddonOrder` 仅 seat 要订阅;customer/translate/tokens 免订阅按包计价,核销 `grantPack` 发放到永久表。**订阅/续费单可搭售扩展包**(CreateOrderCmd.packs,合计含包,核销 `grantBundledPacks`;orderSign 纳入 addon_packs 防篡改)。
    - **安全/校验**:核销前校验订单 HMAC 签名(防库改金额);金额全后端算;续费/升级加购席位不得低于当前订阅(前端 seatFloor + 后端兜底)。
    - **后管**:`grantSubscription` 去 extraCustomers;新增 `POST .../subscription/resource` 调整扩展额度(setPack);订阅详情资源总量走 QuotaService;**项目管理列表加「订阅套餐」列**(planName+过期标识,AdminViewService 走 BillingService 填充)。
    - **前端**:概览扩展包读真实值/免订阅可买;SubscribeModal+AddonModal 加翻译/Tokens/客户**规格下拉**(档位×1/2/5/10/50)、周期/席位**步进器**(console+admin 统一)、订单记录补 addon_translate/tokens 类型与资源展示(按万)。
  - **console 计费前端 ✅完成**(commit 6c77a42 闭环 + a799c4f 加购/筛选):概览(资源用量+加购入口+项目卡)、套餐订阅(卡片+SubscribeModal下单/续费/升级)、订单记录(筛选+去支付/取消+PendingPayModal),下单→二维码收款→Coinly回调/余额核销 全闭环。
  - **加购(席位/客户配额)✅完成**(commit 65e3f5e 后端 + a799c4f 前端):V23 给 bil_order 加 resource_type/quantity/period_days、bil_subscription 加 extra_customers、种子客户拓展包 pack_customer_1k。`addon_seat` 按订阅剩余天数折算(单席位月价×数量×剩余天/30)只加 seats 不改到期;`addon_customer` 永久配额包(每包价×包数)加 extra_customers。加购前置=有有效订阅。接口 GET /billing/addon/quote、POST /billing/order/addon;/orders 加 类型/状态/日期/订单号筛选;/usage 客户配额=套餐+加购。**扩展服务(翻译包/AI Tokens)按设计不展示**(功能未做)。
  - **①套餐展示+概览 ✅完成**:V19 建 bil_subscription/order/wallet/recharge/address 表;BillingController GET /billing/plans + /overview;前端 服务订阅菜单(概览/套餐订阅/订单记录)+ BillingPlans 卡片(居中/hover边框/code本地化)+ BillingOverview。套餐只放现有功能(features码),资源只算 seat/customer。
  - **②建址充值+回调入账 ✅完成**:V20 建 bil_coin 币种配置表(后管可配,种子 USDT TRC20/ERC20)+ V19 bil_wallet 加 version 乐观锁(注:V19 已apply,version 列改放 V20 的 ALTER,V19 还原避免校验和不匹配)。已落地:
    - framework 安全工具:`AesGcmUtil`(AES-256-GCM 地址加密,IV随机前置)、`HmacUtil`(HMAC-SHA256 行签名+常量时间比对)、`DigestUtil`(md5Hex/sha256Hex)。
    - 渠道抽象:`PaymentChannel` 接口 + `PaymentChannelRegistry`(按 channelKey 索引)+ `CoinlySigner`(MD5 签名:排空值→key升序→k1v1→apiKey前置)+ `CoinlyChannel`(JDK HttpClient 建址,无第三方jar)。
    - service:`BillingAddressService`(项目每链固定地址,chain维度锁内"查→建→AES加密入库",address_hash 供反查)、`BillingWalletService`(回调入账:验签→status=1→address_hash反查项目→项目锁→事务内 txid幂等+原子自增+version乐观锁+行HMAC)。
    - 接口:坐席端 `GET /billing/coins`·`GET /billing/wallet`·`POST /billing/address?currency=`;回调 `POST /api/public/billing/coinly/callback`(返回纯文本 success)。
    - **联调待校正(用 gateway.hhnumilala.xyz)**:bil_coin 的 chain_id(tron/ethereum 占位)/token_id 需调 Coinly `/v1/coins` 拿真实值;回调 ack 串(暂返回 "success")按 Coinly 实际期望确认;回调参数 amount/数值字段签名以字符串拼接,需核对 Coinly 实际 JSON 类型。
  - **③下单核销开通 ✅完成**(后端):V21 给 bil_order/bil_subscription 加 seats 列。`BillingOrderService`:createOrder(校验套餐上架+非定制→算价((套餐月价+席位包单价×加购席位)×月数)→判定 type new/renew/upgrade→lock:bil:order:{pid} 作废旧待支付单建新单)、payOrder(lock:bil:wallet:{pid}+事务内原子扣款 version乐观锁+balance>=amount条件+markPaid 状态条件更新+开通订阅)。接口 POST /billing/order·/order/pay,GET /order/pending。**前端下单弹窗(图65)未做**。
  - **④升级/续费 + 订单记录 ✅完成**(后端):续费=原到期叠加;升级/过期重购=换套餐次日重新起算(MVP 不做差价折算)。GET /billing/orders 分页倒序。**前端订单记录页(图63)未做**。
  - **⑤配额计量 + 到期处理 ✅完成**(后端):MemberService.countActiveMembers/CustomerService.countByProject 真实计量;GET /billing/usage(席位=成员数,客户=客户数 vs 套餐配额+加购席位);SubscriptionExpiryJob 每10分钟 tryExecute 将到期订阅置 status=0。**前端 usage 展示/到期降级拦截 未做**。
  - **后管(admin 8090) ✅完成**(commit fb09d2e 后端 + 1ed9b1e 前端):订单管理 GET /api/admin/orders(跨项目分页**倒序**+projectId/status/type 过滤+补项目名,权限 orders);币种管理 GET/POST/PUT status/DELETE /api/admin/coins(bil_coin CRUD,currency 渠道内唯一,权限 orders)。**前端 apps/admin 已有完整脚手架(登录/布局/用户/项目/角色/套餐/加量包/协议/语种页)**,本次补 Orders.tsx(订单列表倒序+过滤)+Coins.tsx(币种CRUD,照Addons范式);分页字段统一 page。
  - **关键设计(新会话必读)**:
    - 计费=**项目固定地址(每链一个)+订单金额匹配+余额兜底防丢钱**(同 cicada);UI 照 ByteTrack;只卖现有功能,架构数据驱动(features码/resource_type 字符串/PaymentChannel 抽象,加东西不改码)。
    - **Coinly 接口契约(官方文档确认)**:POST,Content-Type application/json,参数全在 body(含 pid/nonce(6位随机)/timestamp(毫秒)/sign)。**签名规则**:排除 sign 和空值 → 参数按 key 字典序升序 → 拼成 `key1value1key2value2`(无分隔符)→ **apiKey 前置**(直接拼最前面)→ MD5 转小写。建址 `POST {base}v1/address/create` body{pid,chain_id,callback_url,alias,nonce,timestamp,sign},返回 address;base=https://gateway.hhnumilala.xyz/api/(含/api/,拼 v1/...)。回调字段:pid/cid/chain_id/token_id/currency/amount/address/status(1成功)/txid/block_height/block_time/nonce/timestamp/sign。文档:https://mint-a324815e.mintlify.app/docs/
    - 配置在 application.yml `aitalky.billing.*`(channel/coinly base-url/pid/api-key/callback-url + sign-key HMAC密钥 + address-key AES密钥)。测试 pid=2067074671589957634 apiKey=ExDW1GnTccSWzCYOtMBS7TPZMwvF6dlw。
    - **并发(必须做)**:余额入账用项目锁 lock:bil:wallet:{pid} + 原子 balance=balance+? + version乐观锁 + txid幂等;下单用 lock:bil:order:{pid} 保证唯一待支付;核销用状态条件更新 UPDATE...WHERE status=0。
- [~] 平台后管(admin)后端:登录(pf_admin 用户名+RSA密码+图形验证码)/用户/项目/套餐/加量包/协议/**语种字典 pf_language**/**订单管理(跨项目倒序+过滤+补项目名)**/**币种管理(bil_coin CRUD)** 接口全通(8090,默认 admin/admin123456,权限码 orders);**订阅查看/手动开通/停用/调整扩展额度 ✅已补**(2026-06-18)。详见 `doc/后管功能清单.md`

## 三、前端(坐席台 console)
- [x] 登录(两步式2FA)/注册 / 项目选择创建 / 进入项目
- [x] 工作台外壳(图标栏 + 项目切换 + 头像菜单 + 暗色/语言切换)按 ByteTrack 对齐
- [x] 收件箱三栏结构 + 分类视图 + 会话列表头/Segmented(空态)+ 配色列宽对齐
- [x] 设置布局(分组菜单,菜单隔离)+ 成员管理页
- [x] **收件箱接真数据**:会话列表(视图+进行中/已结束+10s轮询兜底)+ 聊天区(消息流/发送/内部消息/认领/结束)+ 详情面板(客户/会话资料);WS 推送命中实时上屏
- [x] **WS 客户端封装**(连接/心跳25s/指数退避重连/订阅会话+重连补订阅)+ 接入聊天区(进入项目自动建连)
- [x] **漏消息全自动补偿(根治cicada坑)**:后端 VO 暴露 lastSeq;前端 localMaxSeq(容忍空洞)+ 按seq去重有序插入 + 三道网(重连/gap/focus)+ serverLastSeq对账,无感不刷新。详见 `doc/design/消息可靠投递与漏消息补偿.md`
- [x] **信使端 app(apps/messenger)**:URL ?appId&userId 接入 → init → 首页问候 + 聊天窗(消息流/发送/WS实时/同套补偿),按 ByteTrack 20-url-zh / 23-userid 对齐
- [x] 详情面板 1:1 对齐 ByteTrack(详情标题+横排头像+两行式字段+分组折叠+加黑按钮);联系方式/邮箱可编辑(PUT /conversations/{id}/customer);语言/会话分配(坐席名)已接真数据
- [~] **详情面板数据补齐(字段位+展示链路已通,差 1 个数据源)**:
  - 所在地 location —— 字段链路已通(OpenConversationCmd→CnvConversation.location→DetailVO→前端渲染),但**后端无 GeoIP 自动解析**(setLocation 直接用 init 传入值,信使端不传则显示空)。**仅差 IP→地区解析**。
  - ~~来源渠道/渠道名称~~ —— ✅ 已做(`channelName = assignService.groupName(groupId)`,详情面板 + 信使头部均渲染)
  - ~~加入黑名单~~ —— ✅ 已做(BlacklistController.blockCustomer)
- [x] 聊天区输入区 1:1 对齐 ByteTrack(tab+自动翻译开关+客户源语言 / 加高输入框 / 工具栏图标 + 翻译·发送按钮)
- [ ] **聊天区输入区占位功能落地(UI已摆好,缺后端)**:
  - 自动翻译开关 + 翻译按钮 —— 依赖 AI翻译模块(实时翻译+译文缓存+计费扣字符)
  - 工具栏:~~图片/附件~~ ✅已做(上传/暂存预览/发送/渲染,type=image/video/file)、~~快捷回复~~ ✅已做;**链接、知识库(wiki) 未做**
  - 顶部「将客户消息翻译为…」提示条(暂未加)
  - 消息 hover 操作(复制/撤回)、系统提示消息(如"会话被重新分配给X")
- [x] 收件箱分类计数(各视图进行中数量,GET /conversations/counts)
- [x] 历史消息向上翻页:**两端后端/前端拉取逻辑对称**(首屏都 `loadLatest(50)`,增量都 `sync(afterSeq)`,均无 `beforeSeq` 翻页)。现象上**坐席端更受影响**——每次切换会话重新只取最近50,打开老会话看不到更早消息;信使端长期驻留同一会话靠 WS/增量累积所以显示得多。需后端加 `beforeSeq` 历史分页 + **坐席端**(优先)上滑触底加载前插。(已读回执 / mention 视图 ✅已做)
- [x] **服务订阅**设置页(概览/套餐订阅/订单记录)全填实(含加购);信使设置(Messenger/UrgentNotice/ConversationSettings/API管理)、角色管理(RolePage)、团队基本信息、注销项目 等均已填实;**仅「数据管理」路由 `/settings/data` 仍是 Placeholder 占位**(待填实)
- [~] 平台后管前端(apps/admin):脚手架/登录/布局/用户/项目/角色/套餐/加量包/协议/语种/**订单/币种** 页已落地;**项目订阅抽屉(查看+手动开通/试用+停用+调整扩展额度)+ 项目列表「订阅套餐」列 ✅已补**(2026-06-18)。

## 四、当前正在做 / 下一步
- **已完成(大块)**:P3 收件箱接真数据 + WS 封装 + 漏消息补偿 + 信使端 messenger + 信使配置 + 客服组分配(普通/专属)+ 个人中心 + 黑名单 + 快捷回复 + 分类计数 + 头像上传 + **订阅计费全套(5期+加购+扩展包全做+统一配额校验)+ 平台后管(订单/币种/订阅管理)**。
- **环境**:app(8080)/ws(9100)/console(5173)/messenger(5174)+ 中间件 均在本机运行。appId:我的客服项目=`BkbMV3jWNx`。库快照/清库脚本见 `aitalky-parent/sql/`。
- **真正待开发(按优先级)**:
  1. **AI 翻译**(最大块,且解锁前端一堆占位):客户消息实时翻译 + 译文缓存 + 计费扣字符(QuotaService 已就绪可接 translate/tokens 计量)→ 随后接通自动翻译开关/翻译按钮/翻译提示条。
  2. **详情面板「所在地」**:仅差后端 GeoIP 解析 IP→地区(字段/展示链路已通;来源渠道名 ✅已做)。
  3. **收件箱/消息补强**:历史消息向上翻页、消息 hover 复制、聊天工具栏 链接/知识库(图片/附件、已读回执、mention、会话超时系统消息、铃声提醒 ✅均已做)。
  4. **数据管理设置页**(`/settings/data` 当前 Placeholder 占位;信使设置/角色管理等其余设置页已填实)。
- **上线前(见第五部分)**:配置改环境变量、关万能码、换 RSA/JWT 密钥、清 git 历史明文、计费 Coinly 真实环境联调校正(chain_id/token_id/回调 ack)。
- 建议顺序:AI 翻译 → 详情数据源 → 收件箱/工具栏补强 → 设置页填实 → 上线前收尾。

## 五、待清理
- [ ] ws PushService/MessagePushListener 的 info 日志按需降级
- [ ] 成员管理表格等剩余硬编码中文文案补 i18n
- [ ] 上线前:配置改环境变量、关万能码、换 RSA/JWT 密钥、清理 git 历史中的明文密钥
