-- ============================================================
-- 订阅计费:订单 24h 支付有效期(对齐"待支付"弹窗倒计时 + 订单记录"订单有效期24小时")
-- 待支付订单超时未付 → 作废;前端展示剩余支付时间。
-- ============================================================

ALTER TABLE `bil_order`
  ADD COLUMN `expire_time` datetime DEFAULT NULL COMMENT '待支付订单过期时间(下单+24h)' AFTER `paid_time`;
