-- ============================================================
-- 订阅计费:订单绑定收款网络
-- 下单时选定的链(USDT-TRC20/USDT-ERC20)落库到订单,收款地址由该链项目固定地址决定;
-- 「去支付」复用此链,不再允许切换网络。旧单 pay_currency 为空(去支付时回退首个可用币种)。
-- ============================================================

ALTER TABLE `bil_order`
  ADD COLUMN `pay_currency` varchar(32) DEFAULT NULL COMMENT '下单选定收款网络(如 USDT-TRC20),决定收款地址所在链;旧单为空' AFTER `currency`;
